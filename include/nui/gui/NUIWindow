#ifndef __NUI_WINDOW__
#define __NUI_WINDOW__

#include <ncurses.h>
#include <NUIObject>

namespace NUI {
   
	class NUIWindow : public NUIObject {
	public:
		NUIWindow(NUIObject* parent = nullptr);
		~NUIWindow();
		void paintEvent(NUIPaintEvent* event) override;

		WINDOW* m_pWin;
	};
}
#endif